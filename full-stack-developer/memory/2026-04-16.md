# 2026-04-16 日记

## 完成了什么

### 校园运动场地预约系统 - GCP 部署完成
- 后端部署到 Cloud Run (Java Spring Boot)
- 前端部署到 Cloud Run (Vue 3 + nginx)
- Cloud SQL 数据库已创建并导入初始数据
- Artifact Registry 镜像仓库已配置

**服务地址**:
- 后端: https://backend-385311213796.asia-east1.run.app
- 前端: https://frontend-385311213796.asia-east1.run.app

**测试账号**: admin / admin123 (登录成功)

## 遇到的问题和解决

1. **Docker 镜像构建失败**: Alpine + Maven 兼容性问题
   - 解决: 改用 `eclipse-temurin:21-jdk` + `apt-get install maven`

2. **Lombok 注解处理器不生效**: 
   - 解决: 添加 `import com.baomidou.mybatisplus.annotation.FieldFill;`

3. **前端 TypeScript 路径别名**: `tsconfig.app.json` 缺少 `baseUrl` 和 `paths` 配置
   - 解决: 添加正确的路径别名配置

4. **Cloud Run 连接 Cloud SQL 失败**: 
   - 解决: 使用公共 IP 直接连接 (临时方案)

5. **Firebase CLI 无法认证**: 
   - 解决: 改用 Cloud Run 部署前端

## 待处理
- student001 账号密码重置
- 恢复 Cloud SQL 访问控制 (当前 0.0.0.0/0 有安全风险)
- 考虑添加自定义域名

## 下午完成的工作

### UI 重新设计
- 登录页：活力明亮主题，橙绿渐变背景
- 主布局：白色侧边栏 + 用户角色标识
- 工作台：渐变统计卡片 + 挥手动画

### Deploy Master Skill
- 创建位置：`/root/.openclaw/skills/deploy-master/SKILL.md`
- 包含：Dockerfile 模板、cloudbuild.yaml、部署命令、CI/CD 配置

### GitHub Token
- 已保存到 MEMORY.md
- Token: ghp_REDACTED

### 系统命名
- 启舟 - AI 部署助手
